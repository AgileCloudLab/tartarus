def build(bld):
    bld(features = 'cxx cxxprogram',
        target = 'raw_data_test',
        source = 'raw_data_test.cpp',
        includes=['../../include/', '../../src/'],
        lib=['gtestmock'],
        libpath=['lib'],
        use = ['tartarus'])
